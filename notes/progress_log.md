# Progress Log

Append a dated entry after every significant session. Don't rewrite
history — if something was tried and rejected, record that.

---

**2026-09-14** — Project restarted from scratch (v2). Abandoned the
reliability-aware Bayesian twin / patent-novelty architecture entirely.
New direction: digital twin (simple evolving state) is the centerpiece,
ML (RF + XGBoost trained in scikit-learn) supplies the actual prediction,
three demo layers (single prediction / evolving twin with add-visit /
simulation with what-if scenarios). New fully-synthetic dataset generated
(400 patients, 2,196 visits) — see `data_dictionary.md`. Folder structure
and doc set (architecture.md, api_contract.md, antigravity_brief.md)
rebuilt for v2. No code implemented yet.

**2026-09-14 (Backend Scaffolding)** — Scaffolded the FastAPI backend and test environment. Created `api/schemas.py` using Pydantic to strictly enforce the API contract and `api/main.py` with route stubs for the core endpoints. Added an in-memory synthetic dataframe fixture to `tests/conftest.py` to enable fast unit testing without disk I/O, and created a `.gitignore` to keep model artifacts and caches out of version control.

**2026-09-14 (Data Preparation & Preprocessing Pipeline)** — Implemented `src/data_prep.py` and `tests/test_data_prep.py`. Established strict feature leakage safeguards by explicitly dropping downstream consequence fields (`spo2_pct`, `cat_score`, `injected_issue`, `fvc_liters`, `fev1_fvc_ratio`), restricting the model input matrix exclusively to the 9 approved features (`age_at_visit`, `sex`, `gold_stage_baseline`, `smoking_status_at_visit`, `pack_years`, `activity_level_at_visit`, `bmi`, `months_since_baseline`, `baseline_fev1_liters`). Implemented patient-level train/test splitting (`split_by_patient`) by `patient_id` to prevent longitudinal data leakage. Implemented cleaning logic to drop synthetic outlier FEV1 readings (`injected_issue == 'outlier_fev1'`) and handle missing consequence values. Built `build_preprocessor` utilizing a scikit-learn `ColumnTransformer` with `OneHotEncoder(handle_unknown='ignore', sparse_output=False)` configured to output pandas DataFrames. Verified all 9 unit tests pass in `pytest tests/test_data_prep.py`.

**2026-09-14 (Model Training Pipeline & Inference)** — Implemented `src/model.py` and `tests/test_model.py`. Used `GroupShuffleSplit` on `patient_id` to strictly guarantee zero patient overlap between train and test sets (320 train patients / 80 test patients, 1709 train samples / 455 test samples). Trained both a Random Forest regressor and an XGBoost regressor predicting `fev1_liters`. Evaluated both models on the held-out patient test set:
- Random Forest: MAE = 0.0543, RMSE = 0.0681, R² = 0.9931
- XGBoost: MAE = 0.0530, RMSE = 0.0667, R² = 0.9934
Both models demonstrated high fidelity on the synthetic trajectory curves, with XGBoost slightly outperforming Random Forest across MAE and RMSE. Saved fitted preprocessor, trained models, and metadata into `artifacts/` (`preprocessor.joblib`, `rf_model.joblib`, `xgb_model.joblib`, `model_metadata.joblib`). Exposed `predict_fev1(features, model_type)` supporting single-patient and batch inputs with in-memory artifact caching. All 8 tests in `tests/test_model.py` and all 17 tests in the full test suite passed cleanly.

**2026-09-14 (Digital Twin State Module)** — Implemented `src/twin_state.py` and `tests/test_twin_state.py`. Built the deliberately simple `TwinState` class maintaining `patient_id`, `static` baseline characteristics, `current` visit observation, and complete `history` longitudinal visit list. Strictly separated state management from machine learning logic (no prediction, no simulation, and no Bayesian updating). Implemented `add_visit(new_visit)` to append observations to `history` and update `current`, with deep copy isolation. Added helper constructors `from_dict` and `from_dataframe`. Verified all 6 unit tests in `tests/test_twin_state.py` and all 23 tests across the entire test suite pass cleanly.

**2026-09-14 (Trajectory Simulator & What-If Engine)** — Implemented `src/simulator.py` and `tests/test_simulator.py`. Built `simulate_trajectory` which sweeps `months_since_baseline` forward in increments of `step_months` up to `horizon_months` from a `TwinState` instance. Automatically advances patient age with elapsed simulation time. Employs batch prediction via `predict_fev1` for performance. Implemented what-if scenario support for standard clinical interventions (`baseline`, `smoking_cessation`, `increased_activity`) and custom feature override dictionaries. Returns structured output matching API contract `SimulateResponse`. Added 9 unit tests in `tests/test_simulator.py`. Verified all 32 tests in the entire test suite pass cleanly.

**2026-09-14 (Model Evaluation & Comparison Module)** — Resolved API schema naming mismatch by creating canonical `api/schemas.py` and aliasing `api/schemes.py`, restoring `api/main.py` importability. Implemented `src/evaluation.py` and unit tests in `tests/test_evaluation.py`. Designed functions to load trained models (`rf_model.joblib`, `xgb_model.joblib`), preprocessors, and metadata from `artifacts/`. Implemented dual-mode metric extraction supporting fast pre-computed metadata loading as well as dynamic recalculation over custom evaluation DataFrames. Built descending-sorted feature importance extraction for both Random Forest and XGBoost. Formatted outputs to strictly validate against the `ModelComparisonResponse` Pydantic schema for the `/models/comparison` route. Added 10 unit tests in `tests/test_evaluation.py`. Verified all 10 evaluation tests and all 42 tests across the complete test suite pass cleanly.

**2026-09-14 (FastAPI Routing & Integration Testing)** — Implemented full routing in `api/main.py` connecting the API contract directly to our tested ML and simulation modules. Wired up `/predict` using `predict_fev1` from `src.model`, `/simulate` using `simulate_trajectory` from `src.simulator`, and `/models/comparison` using `get_model_comparison_response` from `src.evaluation`. Implemented an in-memory `PATIENT_DB` pre-populated with synthetic digital twins (loading all 400 patients from `data/raw/copd_synthetic_longitudinal.csv` in ~0.3s) powering `/patients`, `/patients/{patient_id}`, and `/patients/{patient_id}/visits`. Configured `CORSMiddleware` for Next.js frontend communication and added `httpx` to `requirements.txt`. Created 15 integration tests in `tests/test_api.py` using `fastapi.testclient.TestClient`. Verified all 15 API tests and all 57 tests across the entire test suite pass cleanly with zero regressions.

**2026-09-15 (Next.js Dashboard & Core Three-Layer Layout)** — Scaffolding the Next.js 16 (App Router + TypeScript) frontend in `dashboard/`. Built a medical clean aesthetic design system in `app/globals.css` using clinical teal (`#0d9488`), slate blue (`#2563eb`), white card surfaces, soft grey borders, and typography. Installed and configured `recharts` for clinical trajectories and `lucide-react` for iconography. Implemented the API client (`lib/api.ts`) pointing to `http://localhost:8000`. Configured 3 pre-seeded 'Golden Demo' archetypes (`lib/goldenPatients.ts`) including Rapid Decliner (`P0124`), Stable Maintenance (`P0040`), and Intervention-Sensitive (`P0001`). Built the complete interactive application layout across all three demo layers: Layer 1 Single Prediction form (`components/Layer1Prediction.tsx`), Layer 2 Evolving Twin with visit timeline and live 'Add Visit' form (`components/Layer2EvolvingTwin.tsx`), and Layer 3 What-If simulation with diverging trajectories and model comparison metrics (`components/Layer3Simulation.tsx`). Verified production build with `npm run build` compiling with zero errors.

**2026-09-18 (SQLite Persistence, Vitality Command Center & Digital Twin Onboarding)** — Upgraded the platform into a persistent software product with an interactive cockpit:
- Implemented SQLite database layer (`api/database.py`) using SQLAlchemy 2.0 with automatic seeding from `data/raw/copd_synthetic_longitudinal.csv` on FastAPI startup.
- Implemented `POST /patients` to onboard new digital twins with automatic GOLD stage inference and baseline trajectory initialization.
- Implemented `GET /patients/{id}/health-score` dynamically calculating a composite 0–100 Twin Health Score, annual FEV1 decline rate (mL/yr), "Years until GOLD Stage IV" countdown, and clinical report-card grades (A–F).
- Implemented `GET /cohort/percentile` computing patient lung function rank against the 400-patient synthetic cohort.
- Built `CreateTwinModal.tsx` in Next.js with real-time FEV1/FVC ratio and GOLD stage calculation, plus instant selection upon creation.
- Built `VitalityCommandCenter.tsx` in Layer 2 featuring an SVG radial gauge, milestone countdown, cohort percentile banner, and multi-domain report card.
- Created 9 integration tests in `tests/test_persistence_and_vitality.py`; full suite passes cleanly with 76/76 passing tests. Verified production Next.js build.

**2026-09-18 (Day 2: Tactile Interactivity & Clinical Memory)** — Executed full interactive frontend enhancements:
- Live Slider Playground (`components/Layer1Prediction.tsx`): Replaced manual inputs for `pack_years`, `bmi`, and `activity_level_at_visit` with tactile range sliders; implemented 300ms live debounced model inference updating FEV1 and composite Health Score (0–100) instantly without submit clicks.
- Automated Clinical Audit Trail (`components/AuditTrail.tsx` & `components/Layer2EvolvingTwin.tsx`): Built vertical scrolling timeline with automated plain-English clinical notes on every visit (e.g. "Visit 4 Processed: FEV1 decreased by 0.15L. Risk shifted to High"), plus a "Just Recorded" memory banner.
- Live Alert Feed (`context/ToastContext.tsx`): Implemented floating high-contrast clinical toast notification feed triggered when exacerbation risk > 60%, FEV1 drops to GOLD Stage IV (<= 0.90L), or acute flares occur.
- Resource ROI Badges (`components/Layer3Simulation.tsx`): Computed scenario deltas translating simulation math into clinical value badges (e.g. "Smoking Cessation preserves 0.30L of FEV1 capacity", "Delays severe impairment by 3.8 years", "Attenuates 12-month acute risk by -28.5%").
- Verified with `npm run build` (0 TypeScript errors) and `pytest` (76/76 passed).